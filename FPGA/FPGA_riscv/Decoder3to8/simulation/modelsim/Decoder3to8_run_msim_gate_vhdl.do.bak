transcript on
if {[file exists gate_work]} {
	vdel -lib gate_work -all
}
vlib gate_work
vmap work gate_work

vcom -93 -work work {Decoder3to8.vho}

vlog -vlog01compat -work work +incdir+X:/altera/13.1/quartus/project {X:/altera/13.1/quartus/project/Decoder3to8_tb.v}

vsim -t 1ps +transport_int_delays +transport_path_delays -sdftyp /NA=Decoder3to8_vhd.sdo -L altera_mf -L altera -L lpm -L sgate -L cycloneiv_hssi -L cycloneiv_pcie_hip -L cycloneiv -L gate_work -L work -voptargs="+acc"  Decoder_3to8_tb

add wave *
view structure
view signals
run -all
